services:
  app:
    build: .
    image: nodejs
    ports:
      - "5000:api"
#  web:
#    image: nginx:alpine
#    ports:
#      - "80:80"
#    restart: on-failure
#    volumes:
#      - ./client:/usr/share/nginx/html
#      - ./nginx.conf:/etc/nginx/conf.d/default.conf
